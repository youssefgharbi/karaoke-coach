import { PageSection } from "../../../components/ui/PageSection";

export function LyricsTimelineEditor() {
  return (
    <PageSection title="Timed lyrics" description="Each lyric line will have its own order and start/end timestamps.">
      <p>Next step: build a timeline editor with add, edit, remove, and drag-to-reorder support.</p>
    </PageSection>
  );
}
